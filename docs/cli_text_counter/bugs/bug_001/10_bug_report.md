<!--
このファイルはバグ報告書のひな形です。
AIは、この見出し構成を維持してバグ報告書を作成してください。
不要な章がある場合も削除せず、「今回は対象外」と記載してください。
この段階では原因を断定しないでください。修正もしないでください。
-->

# バグ報告書

## バグID

bug_001

## 対象 command/app

cli_text_counter

## 対象 feature

text_counter

## 報告日

2026-05-31

## 報告者

Yoshihiro Arimoto

## 現象

`cli_text_counter` に空文字列を渡したとき、0 を出力することが期待されるが、実際にはエラーになる。

## 期待動作

空文字列は有効な入力として扱い、文字数として 0 を出力する。

## 実際の動作

エラーになる。

## 再現手順

1. 以下のコマンドを実行する:
   ```
   python src/cli_text_counter/entrypoint.py --text ""
   ```
2. 0 が出力されることを期待する
3. 実際にはエラーになる

## 入力例

```text
（空文字列）
```

## 出力例

```text
0
```

## エラーログ

```text
現時点では未取得
```

## 発生条件

- `--text` オプションに空文字列 `""` を渡した場合

## 影響範囲

- 空文字列を入力とするすべての利用ケース

## 関連しそうなファイル

- `src/cli_text_counter/entrypoint.py`

## 不足情報

- 実際のエラーログ（エラーメッセージ、スタックトレース）
- エラーが発生するコードの行番号

## この段階で修正しないこと

- 原因を断定しない
- 実装コードを変更しない
- テストコードを変更しない
- 仕様・設計・テスト計画を変更しない
- 再現手順の整理と情報収集に留める
