# レビュー観点

このファイルはレビュー観点の定義です。
レビュー結果や指摘事項はこのファイルに書き込まず、レビュー結果用のファイルに記録してください。

このテンプレートは feature 単体レビュー用です。
entrypoint や結合試験については、feature との責務分担の確認にとどめてください。
command/app 全体の最終確認は `prompts/review_command.md` に委ねます。

## 対象機能

- command/app: `cli_text_counter`
- feature: `text_counter`
- 関数名候補: `count_characters`

入力文字列の文字数を数え、整数として返す feature を対象とします。

## 仕様レビュー

- [ ] 要件があいまいすぎない
- [ ] 入力と出力が明確である
- [ ] 今回やらないことが明記されている
- [ ] feature は文字数算出だけを担当することが明確である
- [ ] CLI引数解析、標準出力、終了コードは entrypoint 側の責務として整理されている
- [ ] 空文字列を正常系として扱い、`0` を返す方針が明記されている
- [ ] 文字数は Python の `len()` 相当で数える方針が明記されている
- [ ] 単語数、行数、ファイル入力、対話式入力を対象外としている

## 関数設計レビュー

- [ ] 仕様を満たせる関数構成になっている
- [ ] 関数の責務が大きすぎない
- [ ] CLI処理とロジック処理が分かれている
- [ ] 共通化候補が提案にとどまっている
- [ ] `count_characters(text: str) -> int` が文字数算出だけを担当する設計になっている
- [ ] 空文字列を受け取った場合に `0` を返せる設計になっている
- [ ] 日本語などのマルチバイト文字も Python の `len()` 相当で扱う前提と矛盾していない

## 関数呼び出し定義レビュー

- [ ] 関数設計と矛盾がない
- [ ] 処理の流れが追いやすい
- [ ] エラー時の流れが明確である
- [ ] `entrypoint.py` が `count_characters(text)` を呼び出す流れになっている
- [ ] `count_characters` が整数を返し、標準出力へ直接書き込まない流れになっている
- [ ] CLI引数不足や `argparse` エラーは entrypoint 側で扱う流れになっている

## テスト設計レビュー

- [ ] 必須要件を確認できる
- [ ] 正常系と異常系が含まれている
- [ ] 単体試験の範囲に収まっている
- [ ] command/app 単位の結合試験計画と混ざっていない
- [ ] 空文字列を正常系として確認する観点がある
- [ ] 日本語などのマルチバイト文字を確認する観点がある
- [ ] CLI引数解析、標準出力、終了コード、`argparse` エラーを feature 単体テストに含めていない

## 実装レビュー

- [ ] 仕様にない機能を追加していない
- [ ] 関数設計と対応している
- [ ] 関数呼び出し定義と対応している
- [ ] 初学者が読める実装になっている
- [ ] 外部ライブラリを使っていない
- [ ] Python の `len()` 相当で文字数を数えている
- [ ] feature に標準出力やCLI引数解析の責務が入っていない
- [ ] 単語数、行数、ファイル入力、対話式入力に対応する処理を追加していない

## テストレビュー

- [ ] テスト設計の観点が反映されている
- [ ] テスト名がわかりやすい
- [ ] 単体試験の範囲に収まっている
- [ ] `count_characters(text: str) -> int` を対象にしている
- [ ] 空文字列が `0` を返すことを確認している
- [ ] 日本語などのマルチバイト文字を Python の `len()` 相当で確認している
- [ ] CLI引数解析、標準出力、終了コード、`argparse` エラーを feature 単体テストに含めていない

## 共通化提案レビュー

- [ ] 共通化候補がある場合、理由が明記されている
- [ ] 影響を受ける機能が明記されている
- [ ] 人間レビュー前に `src/common/` へ切り出していない
- [ ] `src/common/` が保護対象であることを前提にしている
- [ ] 共通化が不要な場合は、不要であることを説明できる

## 最終確認

- [ ] 仕様・設計・実装・テストに大きなズレがない
- [ ] entrypoint や結合試験の確認は、feature との責務分担の範囲にとどまっている
- [ ] command/app 全体の最終確認は `prompts/review_command.md` に委ねる前提になっている
- [ ] CI/CD、デプロイ資産を追加していない
- [ ] 変更したファイルと理由を説明できる
- [ ] レビュー結果や判定をこのファイルに書き込んでいない

## 補足

レビュー結果はこのファイルではなく、必要になった時点で `25_review_result.md` に記録します。

## 実装着手承認欄

<!--
この feature は既に実装済みの完成済みサンプルです。
上記のチェック項目はレビュー観点の定義として未チェックのまま残しています。
実装着手承認欄のみ、サンプル作成済みのため承認済み状態として配置しています。
-->

- [x] 仕様（20_spec.md）を確認した
- [x] 設計（21_design.md）を確認した
- [x] 呼び出しフロー（22_flow.md）を確認した
- [x] テスト計画（23_test_plan.md）を確認した
- [x] レビュー観点（24_review_checklist.md）を確認した
- [x] 未決事項・人間判断待ちが残っていないことを確認した
- [x] 実装対象ファイルとテスト対象ファイルを確認した
- [x] 上記をすべて確認し、feature 実装に進むことを承認する
