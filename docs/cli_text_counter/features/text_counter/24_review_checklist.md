# レビュー観点

このファイルは feature 単体レビューで確認する観点を定義します。

レビュー観点のチェックボックスは、レビュー工程で確認済みを示すために使ってよいです。
ただし、詳細なレビュー結果・指摘事項・判定はこのファイルに書き込まず、`25_review_result.md` に記録してください。

実装着手承認欄は人間が実装前に確認する欄です。AIはこの欄にチェックを入れません。

<!--
このチェックリストは完成済みサンプルとして、レビュー観点の確認済み状態を示しています。
詳細なレビュー結果や指摘事項は、このファイルではなく 25_review_result.md に記録します。
実装着手承認欄は、サンプル作成済みのため承認済み状態として配置しています。
-->

entrypoint や結合試験については、feature との責務分担の確認にとどめてください。
command/app 全体の最終確認は `prompts/review_command.md` に委ねます。

## 対象機能

- command/app: `cli_text_counter`
- feature: `text_counter`
- 関数名候補: `count_characters`

入力文字列の文字数を数え、整数として返す feature を対象とします。

## 仕様レビュー

- [x] 要件があいまいすぎない
- [x] 入力と出力が明確である
- [x] 今回やらないことが明記されている
- [x] feature は文字数算出だけを担当することが明確である
- [x] CLI引数解析、標準出力、終了コードは entrypoint 側の責務として整理されている
- [x] 空文字列を正常系として扱い、`0` を返す方針が明記されている
- [x] 文字数は Python の `len()` 相当で数える方針が明記されている
- [x] 単語数、行数、ファイル入力、対話式入力を対象外としている

## 関数設計レビュー

- [x] 仕様を満たせる関数構成になっている
- [x] 関数の責務が大きすぎない
- [x] CLI処理とロジック処理が分かれている
- [x] 共通化候補が提案にとどまっている
- [x] `count_characters(text: str) -> int` が文字数算出だけを担当する設計になっている
- [x] 空文字列を受け取った場合に `0` を返せる設計になっている
- [x] 日本語などのマルチバイト文字も Python の `len()` 相当で扱う前提と矛盾していない

## 関数呼び出し定義レビュー

- [x] 関数設計と矛盾がない
- [x] 処理の流れが追いやすい
- [x] エラー時の流れが明確である
- [x] `entrypoint.py` が `count_characters(text)` を呼び出す流れになっている
- [x] `count_characters` が整数を返し、標準出力へ直接書き込まない流れになっている
- [x] CLI引数不足や `argparse` エラーは entrypoint 側で扱う流れになっている

## テスト設計レビュー

- [x] 必須要件を確認できる
- [x] 正常系と異常系が含まれている
- [x] 単体試験の範囲に収まっている
- [x] command/app 単位の結合試験計画と混ざっていない
- [x] 空文字列を正常系として確認する観点がある
- [x] 日本語などのマルチバイト文字を確認する観点がある
- [x] CLI引数解析、標準出力、終了コード、`argparse` エラーを feature 単体テストに含めていない

## 実装レビュー

- [x] 仕様にない機能を追加していない
- [x] 関数設計と対応している
- [x] 関数呼び出し定義と対応している
- [x] 初学者が読める実装になっている
- [x] 外部ライブラリを使っていない
- [x] Python の `len()` 相当で文字数を数えている
- [x] feature に標準出力やCLI引数解析の責務が入っていない
- [x] 単語数、行数、ファイル入力、対話式入力に対応する処理を追加していない

## テストレビュー

- [x] テスト設計の観点が反映されている
- [x] テスト名がわかりやすい
- [x] 単体試験の範囲に収まっている
- [x] `count_characters(text: str) -> int` を対象にしている
- [x] 空文字列が `0` を返すことを確認している
- [x] 日本語などのマルチバイト文字を Python の `len()` 相当で確認している
- [x] CLI引数解析、標準出力、終了コード、`argparse` エラーを feature 単体テストに含めていない

## 共通化提案レビュー

- [x] 共通化候補がある場合、理由が明記されている
- [x] 影響を受ける機能が明記されている
- [x] 人間レビュー前に `src/common/` へ切り出していない
- [x] `src/common/` が保護対象であることを前提にしている
- [x] 共通化が不要な場合は、不要であることを説明できる

## 最終確認

- [x] 仕様・設計・実装・テストに大きなズレがない
- [x] entrypoint や結合試験の確認は、feature との責務分担の範囲にとどまっている
- [x] command/app 全体の最終確認は `prompts/review_command.md` に委ねる前提になっている
- [x] CI/CD、デプロイ資産を追加していない
- [x] 変更したファイルと理由を説明できる
- [x] レビュー結果や判定をこのファイルに書き込んでいない

## 補足

レビュー結果はこのファイルではなく、必要になった時点で `25_review_result.md` に記録します。

## 実装着手承認欄

- [x] 仕様（20_spec.md）を確認した
- [x] 設計（21_design.md）を確認した
- [x] 呼び出しフロー（22_flow.md）を確認した
- [x] テスト計画（23_test_plan.md）を確認した
- [x] レビュー観点（24_review_checklist.md）を確認した
- [x] 未決事項・人間判断待ちが残っていないことを確認した
- [x] 実装対象ファイルとテスト対象ファイルを確認した
- [x] 上記をすべて確認し、feature 実装に進むことを承認する
